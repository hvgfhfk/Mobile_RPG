using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAnimaction : MonoBehaviour
{
    /*Animator anim;

    bool enabledGetDamage;

    private void Start()
    {
        anim = GetComponent<Animator>();
    }

    public void Onattacking()
    { // attacking 
        if(anim.GetCurrentAnimatorStateInfo(0).IsName("Damage"))
        {
            enabledGetDamage = true;
        }
    }
    public void Offattacking()
    {
        if (!anim.GetCurrentAnimatorStateInfo(0).IsName("Damage"))
        {
            enabledGetDamage = false;
        }
    }*/
}
